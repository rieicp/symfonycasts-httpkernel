<div class="navbar-left tweets">
    <p class="text-center">{{ isMozilla ? 'on a Mozilla' : 'Not on a Mozilla' }}</p>
    <p class="text-center">Tweets from T-Rex Problems</p>
    <ul>
        {% for tweet in tweets %}
            <li>{{ tweet }}</li>
        {% endfor %}
    </ul>
</div>
